Source code of this repository

- `bin/` is used for binaries

- `sefunc/` is used for functions expected to setup specified part on the system i.e `00-tor.sh` provides `setup_tor` which configures Tor on target system
